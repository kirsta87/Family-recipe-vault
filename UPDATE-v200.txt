BUILD 200 — COOKBOOK COLUMN BOUNDARY FIX

- Keeps side-by-side ingredient and direction columns separate during PDF text extraction.
- Prevents directions from being merged into ingredient lines.
- Preserves the Build 199 macro parser hardening.
